PORT=5000
MONGODB_URI=mongodb+srv://YOUR_USER:YOUR_PASS@cluster0.xxxxx.mongodb.net/mern_starter
JWT_SECRET=your_super_secret_jwt_key_change_this
CLIENT_URL=http://localhost:5173
BACKEND_URL=http://localhost:5000

# Firebase Admin SDK (download from Firebase Console > Project Settings > Service Accounts)
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_KEY\n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com

# Gmail SMTP for OTP + general notification emails (enrollment/ticket/certificate/payment)
# Use a Gmail account + App Password (not your regular Gmail password)
# Enable: Google Account → Security → 2-Step Verification → App Passwords
GMAIL_USER=iubat21103033@gmail.com
GMAIL_PASS=freiydhbwudexmjg

# SSLCommerz payment gateway (bKash/Nagad/Rocket/card সব একই integration দিয়ে চলে)
# Sandbox account বানাও: https://developer.sslcommerz.com/registration/
# SSLCOMMERZ_IS_LIVE=false রাখলে sandbox e test kora jabe (test card/mobile number দিয়ে)
SSLCOMMERZ_STORE_ID=your_sandbox_store_id
SSLCOMMERZ_STORE_PASSWD=your_sandbox_store_password
SSLCOMMERZ_IS_LIVE=false


lms-role-base-project
frei ydhb wude xmjg
# ─────────────────────────────────────────────────────────
# HOW TO GET GMAIL APP PASSWORD (for OTP emails):
# 1. Go to myaccount.google.com
# 2. Security → 2-Step Verification → Turn ON
# 3. Security → App Passwords → Select app: Mail → Generate
# 4. Copy the 16-char password (e.g. abcd efgh ijkl mnop)
# 5. Use it as GMAIL_PASS (no spaces needed)
# ─────────────────────────────────────────────────────────
